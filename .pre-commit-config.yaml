repos:
  - repo: local
    hooks:
      - id: natsume-check
        name: Natsume lint, Clippy, and tests
        entry: just pre-commit
        language: system
        always_run: true
        pass_filenames: false
