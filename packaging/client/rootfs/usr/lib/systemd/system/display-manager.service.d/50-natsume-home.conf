[Unit]
# The Helper restores and verifies Home before claiming its Type=dbus name.
Wants=natsume-privileged-helper.service
After=natsume-privileged-helper.service
# A root-only, per-boot permit is withdrawn for the entire Home maintenance window.
ConditionPathExists=/run/natsume-privileged/home-ready
