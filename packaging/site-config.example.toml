# Example only; release jobs provide a site-owned, signed input through SITE_CONFIG.
# This file is not copied into a package by default.
schema_version = 1
fleet_namespace_uuid = "REPLACE-WITH-STABLE-SITE-UUID"
gateway_hostname = "gateway.contest.example"
gateway_not_after = "2030-01-01T00:00:00Z"
contest_end = "2029-12-31T00:00:00Z"

[trust]
control_root_sha256 = "REPLACE-WITH-SHA256"
local_origin_root_sha256 = "REPLACE-WITH-SHA256"
