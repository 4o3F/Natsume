# Integration tests

Cross-process, reboot, physical-hardware, protocol, package and fault-injection ownership lives here. v2.5 priority scenarios are identity-before-vault, vault corruption, Device-only Enrollment, mandatory mTLS, Gateway certificate issuance inside authenticated `SYNC_STATE`, explicit state/secret Commands, desktop-only lock, Caddy offline replay and Home recovery.
